import { it } from "vitest";

// Placeholder: this file was used during req-handler debugging and is
// intentionally empty now. Kept (instead of deleted) only because rm is
// not permitted here; the user can remove it when convenient.
it.skip("dbg-handler scaffold (removed)", () => {});
