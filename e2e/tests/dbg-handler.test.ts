import { it } from "vitest";

// Placeholder: used during debugging; intentionally empty. Kept (instead
// of deleted) only because rm is not permitted in this sandbox.
it.skip("dbg-handler scaffold (removed)", () => {});
