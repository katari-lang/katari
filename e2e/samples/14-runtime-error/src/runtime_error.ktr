@"Catches a prim runtime error (division by zero) that the engine emits as a throw."
agent main() -> string {
  use handler {
    request throw(msg: string) {
      break f"engine threw: ${msg}"
    }
  }
  10 / 0
  "unreachable"
}
