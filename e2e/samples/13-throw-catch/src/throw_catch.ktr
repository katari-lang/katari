@"Catches an explicit throw inside a handle scope."
agent main() -> string {
  use handler {
    request throw(msg: string) {
      break f"caught: ${msg}"
    }
  }
  throw(msg = "kaboom!")
  "unreachable"
}
