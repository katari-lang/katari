@"Catches an explicit throw inside a handle scope."
agent main() -> string {
  handle {
    request throw(msg: string) {
      break f"caught: ${msg}"
    }
  }
  throw(msg = "kaboom!")
  "unreachable"
}
