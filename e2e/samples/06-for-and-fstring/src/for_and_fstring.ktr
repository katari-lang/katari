@"Sum 1..3 via for/then with state var, formatted as f-string."
agent main() -> string {
  let total = for (let x in [1, 2, 3], var acc: integer = 0) {
    next with { acc = acc + x }
  } then { acc }
  f"sum = ${to_string(value = total)}"
}
