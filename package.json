{
  "private": true,
  "packageManager": "pnpm@11.1.3",
  "scripts": {
    "prepare": "simple-git-hooks",
    "build": "npm run build:hs && npm run build:ts",
    "build:hs": "stack build",
    "build:cli": "stack build katari",
    "build:lsp": "stack build katari-lsp",
    "build:ts": "pnpm -r --filter \"./typescript/packages/*\" run build",
    "test": "npm run test:hs && npm run test:ts",
    "test:hs": "stack test",
    "test:ts": "pnpm -r run test",
    "clean": "npm run clean:hs && npm run clean:ts",
    "clean:hs": "stack clean",
    "clean:ts": "pnpm -r exec -- rm -rf dist",
    "fmt:hs": "find haskell -name '*.hs' -not -path '*/.stack-work/*' -print0 | xargs -0 -r stack exec --package ormolu -- ormolu --mode inplace",
    "lint:hs": "stack build --fast",
    "fmt:ts": "pnpm biome format --write .",
    "lint:ts": "pnpm biome lint .",
    "check:ts": "pnpm biome ci .",
    "watch:hs": "stack build --file-watch",
    "cli": "stack run katari --",
    "lsp": "stack run katari-lsp",
    "dev": "docker compose up -d --wait db s3mock && concurrently --kill-others-on-fail --names api,web --prefix-colors magenta,green \"pnpm --filter @katari-lang/api-server dev\" \"pnpm --filter @katari-lang/admin-web dev\""
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "concurrently": "^9.1.0",
    "simple-git-hooks": "^2.11.1"
  },
  "simple-git-hooks": {
    "pre-commit": "./scripts/pre-commit.sh"
  }
}
