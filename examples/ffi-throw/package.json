{
  "name": "@katari-examples/ffi-throw",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Typed-throw FFI example. Not built/tested in CI; declares the port dep so the bundler resolves it.",
  "dependencies": {
    "@katari-lang/port": "workspace:*"
  }
}
