{
  "name": "@katari-examples/ffi-blob",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "FFI blob end-to-end example. Not built/tested in CI; declares the port dep so the bundler resolves it.",
  "dependencies": {
    "@katari-lang/port": "workspace:*"
  }
}
