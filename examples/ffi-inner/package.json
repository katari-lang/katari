{
  "name": "@katari-examples/ffi-inner",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "FFI inner-delegation example. Not built/tested in CI; declares the port dep so the bundler resolves it.",
  "dependencies": {
    "@katari-lang/port": "workspace:*"
  }
}
