[package]
name = "replay_probe"
description = "Test fixture: the prelude.replay providers driven end to end over compiled IR (see test/replay-providers.test.ts)."

[dependencies]
packages = []

[runtime]
url = "http://localhost:3000"
