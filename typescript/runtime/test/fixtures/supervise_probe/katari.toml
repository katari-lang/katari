[package]
name = "supervise_probe"
description = "Test fixture: the prelude.supervise providers driven end to end over compiled IR (see test/supervise-providers.test.ts)."

[dependencies]
packages = []

[runtime]
url = "http://localhost:3000"
