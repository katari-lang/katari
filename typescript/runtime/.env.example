# Server
PORT=3000
LOG_LEVEL=info
NODE_ENV=development

# Database — matches compose.yaml. Override the host/port if you map a
# different one via POSTGRES_PORT in Docker Compose.
DATABASE_URL=postgres://katari:katari@localhost:5432/katari
