/**
 * Aggregated Drizzle schema. Re-export every module's tables here so the
 * client (and the relational query API) sees the whole schema in one place.
 */
export * from "../modules/users/users.table.js";
