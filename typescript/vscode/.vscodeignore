node_modules/**
.gitignore
.vscodeignore
.vscode/**
tsconfig*.json
src/**
scripts/**
e2e/**
test/**
tests/**
**/*.ts
**/*.map
katari-vscode.vsix
# The icon ships as PNG (what the Marketplace and Open VSX render); the SVG is the source it is
# rasterized from, so it belongs in the repository and not in the package.
icon.svg
