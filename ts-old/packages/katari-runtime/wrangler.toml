name = "katari-runtime"
main = "src/worker.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

[vars]
KATARI_BASE_URL = "https://katari-runtime.<your-subdomain>.workers.dev/katari"

# Set DATABASE_URL as a secret:
# wrangler secret put DATABASE_URL
